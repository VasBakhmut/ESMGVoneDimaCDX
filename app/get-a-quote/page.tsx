import type { Metadata } from "next"; import { Eyebrow } from "@/components/sections"; import { business,buildingTypes,services } from "@/data/site";
export const metadata:Metadata={title:"Get an ESM Inspection Quote",description:"Request a quote for AESMR preparation, egress inspections, passive fire audits or ESM consultancy in Victoria."};
export default function Page(){return <section className="content-page shell"><Eyebrow>Get a quote</Eyebrow><h1>Tell us what<br/>you’re responsible for.</h1><p className="intro">A few useful details help us respond with the right questions. For something urgent, call <a href={business.phoneHref}>{business.phone}</a>.</p><div className="content-grid"><form className="form-grid"><div className="field"><label htmlFor="name">Name *</label><input id="name" name="name" autoComplete="name" required/></div><div className="field"><label htmlFor="phone">Phone *</label><input id="phone" name="phone" type="tel" autoComplete="tel" required/></div><div className="field"><label htmlFor="email">Email *</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="field"><label htmlFor="building">Building type</label><select id="building" name="building" defaultValue=""><option value="">Select a building type</option>{buildingTypes.map(x=><option key={x}>{x}</option>)}</select></div><div className="field full"><label htmlFor="service">Service needed</label><select id="service" name="service" defaultValue=""><option value="">Not sure / select a service</option>{services.map(x=><option key={x.slug}>{x.title}</option>)}</select></div><div className="field full"><label htmlFor="details">Building address and useful details</label><textarea id="details" name="details" placeholder="Address, building use, number of levels, deadline or concern"/></div><div className="field full"><button className="button" type="submit">Send quote request</button><small>This prototype form is ready to connect to your preferred CRM or email service.</small></div></form><aside><strong>Prefer a callback?</strong><p>Call our office or leave your name and number. We’ll help establish the scope before asking for detailed documents.</p><a href={business.phoneHref}>{business.phone}</a><p>{business.hours}</p></aside></div></section>}
